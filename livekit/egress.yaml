# Reference config for LiveKit Egress (production uses EGRESS_CONFIG_BODY in docker-compose.yml)
api_key: ${LIVEKIT_API_KEY}
api_secret: ${LIVEKIT_API_SECRET}
ws_url: ws://livekit:7880
insecure: true

redis:
  address: redis:6379
